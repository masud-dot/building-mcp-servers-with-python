# Copy to .env and adjust. Never commit .env.
# Every variable is optional; defaults are in config.py.

# How many notes notes://recent returns (1-50).
PRODUCTIVITY_RECENT_NOTES=5

# Largest page list_tasks will return (1-200).
PRODUCTIVITY_MAX_PAGE_SIZE=50
